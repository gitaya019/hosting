import React from 'react'
import ReactDOM from 'react-dom/client'
import FreeTask from './components/FreeTask'
import '../src/styles/Fr.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    < FreeTask />
  </React.StrictMode>,
)
